<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RoomDbDemo" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">
            @android:color/transparent
        </item>
        <item name="android:windowLightStatusBar">true</item> // use light style for status bar (makes icons and texts area dark)
    </style>

    <style name="Theme.RoomDbDemo" parent="Base.Theme.RoomDbDemo" />
</resources>